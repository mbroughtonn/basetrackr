import React from 'react';

function Bench() {
    return (
        <section style={{ marginTop: "20px" }}>
            <h2>Bench & Notes</h2>
            <textarea rows="5" cols="50" placeholder="Bench players, notes, substitutions, etc."></textarea>
        </section>
    );
}

export default Bench;
